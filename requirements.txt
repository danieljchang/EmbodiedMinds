# Combined requirements for the project. Adjust versions as needed for your environment.

# Core ML / DL
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Vision / CV
opencv-python>=4.7.0
matplotlib>=3.7.0
seaborn>=0.12.0
Pillow>=9.5.0

# Data & utils
pandas>=2.0.0
pyyaml>=6.0
tqdm>=4.66.0

# Experiment tracking / logging
wandb>=0.16.0
tensorboard>=2.15.0

# Hugging Face ecosystem
datasets>=2.14.0
huggingface-hub>=0.14.0, <1.0.0
transformers>=4.34.0

# Config / CLI
hydra-core>=1.3.0

# Dev / testing
pytest>=7.4.0
ipython>=8.12.0
jupyter>=1.0.0
black>=23.0.0
flake8>=6.0.0

ultralytics>=8.0.0
timm>=0.10.0

# Optional (models / checkpoints)
# Add GPU-specific torch wheels if needed (e.g., from pytorch.org) instead of the above torch line.